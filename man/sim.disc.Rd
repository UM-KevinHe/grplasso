% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation_Functions.R
\name{sim.disc}
\alias{sim.disc}
\title{Simulate discrete-time survival data with centre effects}
\usage{
sim.disc(
  ls,
  censor_max_t,
  prop.continuous = 0.8,
  prov.size.mean = 80,
  unpenalized.beta = FALSE,
  prop.unpenalized.beta = 0,
  rho = 0.8,
  baseline.Hazard.r = 1.2,
  baseline.Hazard.lambda = 0.01,
  non_integer_time = TRUE
)
}
\arguments{
\item{ls}{a list with elements \code{n.center}, \code{n.beta}, \code{n.time.point},
\code{n.groups}, \code{prop.outlier.center} and \code{prop.NonZero.group}.}

\item{censor_max_t}{the largest possible censoring time.}

\item{prop.continuous}{proportion of covariates that are continuous; the rest
are Bernoulli(0.5).}

\item{prov.size.mean}{mean centre size.}

\item{unpenalized.beta, prop.unpenalized.beta}{as in
\code{\link[=Simulation_data_GroupLasso]{Simulation_data_GroupLasso()}}.}

\item{rho}{correlation between the covariates and the centre effect.}

\item{baseline.Hazard.r, baseline.Hazard.lambda}{shape and scale of the
Weibull baseline hazard.}

\item{non_integer_time}{whether to map the integer event times onto
non-integer values.}
}
\value{
A list with \code{data}, the generating \code{beta}, \code{group}, \code{alpha} and
\code{gamma}, and the column names.
}
\description{
Generates a discrete-time logistic hazard model with a separate effect for
each centre and a Weibull-shaped baseline hazard.
}
\examples{
set.seed(3)
sim <- sim.disc(
  list(n.center = 10, n.beta = 6, n.time.point = 5, n.groups = 3,
       prop.outlier.center = 0, prop.NonZero.group = 0.5),
  censor_max_t = 5, prov.size.mean = 30)
str(sim$data)
table(sim$data[[sim$Event.char]])

}
