% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_simul.R
\name{fe.data.prep}
\alias{fe.data.prep}
\title{Screen and prepare clustered data before fitting}
\usage{
fe.data.prep(data, Y.char, Z.char, prov.char, cutoff = 10, check = TRUE)
}
\arguments{
\item{data}{a data frame containing the outcome, the covariates and the
provider identifier.}

\item{Y.char}{name of the outcome column.}

\item{Z.char}{names of the covariate columns.}

\item{prov.char}{name of the provider identifier column.}

\item{cutoff}{providers with fewer than \code{cutoff} observations are flagged.}

\item{check}{whether to run the screening checks. Default \code{TRUE}.}
}
\value{
The input data with an added \code{included} column.
}
\description{
Checks a data frame for missingness, providers below a size cutoff,
near-zero-variance covariates, pairwise correlation and multicollinearity,
and returns the data with an \code{included} indicator.
}
\section{Optional dependencies}{

The near-zero-variance and VIF checks use \pkg{caret} and \pkg{olsrr},
which are in \code{Suggests} rather than \code{Imports}. If either is not installed the
corresponding check is skipped with a message rather than failing.
}

\examples{
set.seed(5)
sim <- Simulation_data_GroupLasso(
  list(m = 10, n.beta = 6, n.groups = 3, prop.NonZero.group = 0.5,
       prop.outlier = 0, rho = 0.5),
  prov.size.mean = 30)
d <- sim$sim.data
prepped <- fe.data.prep(d, "Y", colnames(d)[3:8], "Prov.ID",
                        cutoff = 5, check = FALSE)
table(prepped$included)

}
