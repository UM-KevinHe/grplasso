% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation_Functions.R
\name{sim.linear}
\alias{sim.linear}
\title{Simulate clustered data with a continuous outcome}
\usage{
sim.linear(m, n.beta, prov.size.mean = 80, rho = 0.7)
}
\arguments{
\item{m}{number of providers.}

\item{n.beta}{number of covariates.}

\item{prov.size.mean}{mean provider size; sizes are Poisson draws floored at 11.}

\item{rho}{correlation between the covariates and the provider effect, and
between any two covariates.}
}
\value{
A list with the simulated \code{data}, the \code{true.beta} and \code{true.gamma}
used to generate it, and the column names \code{prov.char}, \code{Z.char}, \code{Y.char}.
}
\description{
Generates a linear model in which each provider contributes its own random
intercept and the covariates are correlated with that intercept.
}
\examples{
set.seed(1)
sim <- sim.linear(m = 10, n.beta = 5, prov.size.mean = 30)
str(sim$data)
sim$true.beta

}
