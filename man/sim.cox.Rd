% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Simulation_Functions.R
\name{sim.cox}
\alias{sim.cox}
\title{Simulate right-censored survival data with provider effects}
\usage{
sim.cox(
  n.center,
  n.beta,
  prop.zero_beta = 0.1,
  prov.size.mean = 80,
  unpenalized.beta = FALSE,
  prop.unpenalized.beta = 0,
  rho = 0.8
)
}
\arguments{
\item{n.center}{number of providers.}

\item{n.beta}{number of covariates.}

\item{prop.zero_beta}{the proportion of covariates with a NON-zero
coefficient. The name is historical and does not match the behaviour.}

\item{prov.size.mean}{mean provider size.}

\item{unpenalized.beta, prop.unpenalized.beta}{currently unused.}

\item{rho}{correlation between the covariates and the provider effect.}
}
\value{
A list with \code{data}, the generating \code{beta}, and the column names
\code{prov.char}, \code{Z.char}, \code{Time.char}, \code{Event.char}.
}
\description{
Generates survival times from a proportional hazards model in which the
hazard of provider k is \verb{gamma_k * exp(z' beta)}, so \code{gamma_k} acts as a
provider-specific baseline hazard.
}
\section{Known issue}{

The covariates are centred using the mean and standard deviation of the
provider effect in \code{\link[=Simulation_data_GroupLasso]{Simulation_data_GroupLasso()}} (\code{log(4/11)} and \code{0.4})
rather than those of this function's own \code{gamma ~ Uniform(0, 2)}. The
covariate mean is therefore displaced by roughly \code{2*rho} to \code{6*rho}, the
linear predictor inherits a large offset, and the hazard either explodes or
vanishes: measured event rates range from 0.000 to 0.998 depending on \code{rho}
and \code{n.beta}. Standardise \code{gamma} with its own moments (mean 1, standard
deviation \code{sqrt(1/3)}) before relying on this function.
}

\examples{
set.seed(4)
sim <- sim.cox(n.center = 10, n.beta = 6, prov.size.mean = 30)
d <- as.data.frame(sim$data)
# NOTE the event rate: see the "Known issue" section above.  This example
# documents the behaviour as shipped, it does not endorse it.
mean(d[[sim$Event.char]])
fit <- Strat.cox(d, Event.char = sim$Event.char, Z.char = sim$Z.char,
                 Time.char = sim$Time.char, prov.char = sim$prov.char,
                 nlambda = 5)
dim(fit$beta)

}
